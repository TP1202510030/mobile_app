import 'package:flutter/material.dart';

class ActuatorsSection extends StatelessWidget {
  const ActuatorsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Actuadores'),
      ],
    );
  }
}
